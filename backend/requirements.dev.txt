# Development / test dependencies for the CVision backend.
#
# Install with (from the backend/ directory, inside your virtualenv):
#     pip install -r requirements.dev.txt
#
# This pulls in the full runtime stack (-r requirements.txt) so the test suite
# imports the real application, plus the testing tools below.

-r requirements.txt

pytest>=8.0
pytest-cov>=5.0        # coverage reporting: pytest --cov=app
httpx>=0.27            # required by Starlette/FastAPI TestClient
python-dotenv>=1.0     # loads .env.test for the test database connection
