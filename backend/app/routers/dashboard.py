"""
Dashboard router — user dashboard summary and analysis history.
Will be fully implemented in Phase 7.
"""

from fastapi import APIRouter

router = APIRouter(prefix="/dashboard", tags=["Dashboard"])


# Placeholder — Phase 7 will add:
# GET /dashboard/summary  — Get dashboard summary stats
# GET /dashboard/history  — Get analysis history list
